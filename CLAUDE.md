# CLAUDE.md

Memória de trabalho do projeto App de Escala. Leia este arquivo primeiro para pegar o contexto rápido; os detalhes completos ficam em `memoria/memoria.md`.

## Estado atual (2026-08-04)

- Fases 0 a 5 implementadas **e no ar**. Repositório: https://github.com/devsecfate/App-de-escala (branch `main`). Detalhes em `memoria/memoria.md`.
- **Etapa 6 inteira implementada e no ar** (commit `c8e8d12` na `main`; as 7 primeiras migrations estão aplicadas no Supabase `escala-app-sp` e a Vercel publicou). O `planejamento/etapa-6-conta-edicao-redesign.md` está cumprido de ponta a ponta.
- **Excluir a própria conta também está no ar** (commit `250c760`; 8ª migration `20260804180000_excluir_conta.sql` aplicada no `escala-app-sp`). Foi pedida depois do plano da Etapa 6.
- **Tela `/pessoas` e promover a administrador** (9ª migration `20260804200000_promover_admin.sql`). Pedida depois da exclusão de conta. Três buracos que ela fecha:
  - Promover a **líder de ministério** já existia, mas só de dentro da página de cada ministério; agora também sai da lista da igreja, um item de menu por ministério da pessoa.
  - Promover a **administrador da igreja** (`perfis.papel_global`) não existia em lugar nenhum — e a mensagem de `excluir_minha_conta` mandava fazer exatamente isso. Era um beco sem saída.
  - **Não havia nenhuma tela com as pessoas da igreja.** Quem entrava por convite e não estava em ministério algum era invisível para o admin — inclusive o próprio admin, que cria a igreja e não entra em ministério nenhum. A RPC `pessoas_da_igreja()` devolve perfil + vínculos numa consulta.
  - Trigger `perfis_exige_um_administrador`: rebaixar ou desativar o último administrador ativo é recusado. Igreja sem admin não tem conserto por dentro do app, porque não sobra quem promova ninguém.
- **Sessão órfã agora desloga.** Quem exclui a conta num aparelho deixava o outro com um JWT apontando para usuário inexistente, e o outro caía no *onboarding* pedindo código de convite — como se a pessoa fosse nova na igreja. `AuthContext` reconhece `sub claim in JWT does not exist` e chama `signOut()`.
- **Migration antes do deploy, sempre.** As duas publicações desta sessão dependeram disso: o código novo chama RPC e coluna que só existem depois do `npx supabase db push`. Publicar na ordem inversa deixa a Vercel servindo um app que quebra ao carregar. `db push` primeiro, `git push` depois.
- **`mailer_autoconfirm` está `true` em produção** (ligado em 2026-08-04 pela Management API). É o passo da §1.4 do `DEPLOY.md`, e sem ele o `signUp` não devolve sessão: manda um e-mail de confirmação que o SMTP embutido só entrega para o dono do projeto, e ninguém da igreja passa da tela de criar conta. Conferir com:
  `curl -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" https://api.supabase.com/v1/projects/ehwvgsyrtymdypubnhjr/config/auth`
  Ligar de novo, se algum dia voltar: `curl -X PATCH ... -d '{"mailer_autoconfirm": true}'` no mesmo endereço.
- **O service worker serve a versão antiga até um recarregamento.** Depois de publicar, a primeira visita ainda mostra o app velho (é o precache da Fase 5); como o `sw.ts` faz `skipWaiting()` + `clientsClaim()`, o segundo carregamento já traz o novo. Não confunda isso com deploy que não saiu — confira em `api.vercel.com/v6/deployments?app=escala-app`.
- **O que a 6.2 + 6.3 entregaram** (a queixa do usuário era: "interface básica e sem sal, e não dá para excluir nem editar ministério nem função"):
  - **Toda tela do app foi reescrita** em cima dos primitivos: `Home` (cartão da próxima escala com gradiente e glow), `Eventos` (agora "Agenda"), `Ministerios`, `Disponibilidade`, `MinisterioDetalhe`, `MontarEscala`, `Repertorio`, `Relatorio`. Nenhuma tela usa mais `slate` cru.
  - **Editar e excluir existem em tudo:** ministério, evento, função, música, categoria, coluna do repertório, período de indisponibilidade, membro, escala. A regra é sempre a mesma: `decidirExclusao` (em `packages/core/src/exclusao.ts`, com testes) escolhe entre **excluir de vez** (nunca foi usado) e **arquivar** (tem histórico), e escreve a frase que a confirmação mostra — incluindo o número ("já aparece em 6 escalas").
  - **`MinisterioDetalhe` virou quatro seções** em `apps/web/src/components/ministerio/`: `SecaoPessoas`, `SecaoConvites` (a tela de convites que faltava — gerar código, copiar mensagem, mandar no WhatsApp, cancelar), `SecaoFuncoes` e `SecaoRegras`. **O convite por e-mail (Edge Function `convidar-membro`) saiu da tela** — o caminho decidido é o código de convite.
  - Primitivos novos: `MenuAcoes` (o "⋯" de cada item), `Alternador` (Ativos/Arquivados, com pílula deslizante) e `NumeroContando` (os números do relatório subindo de zero). Mais `Cronograma` e `ListaEditavel` como componentes de tela.
  - **Tela nova `/conta`** (`Conta.tsx`): a pessoa corrige o próprio nome/telefone, e o admin corrige nome e **fuso da igreja** — que decide o "amanhã" do lembrete e o "este mês" do relatório e antes era chumbado no onboarding. O nome no header virou o link para ela.
  - **Excluir a própria conta** (migration `20260804180000_excluir_conta.sql`, RPC `excluir_minha_conta()`), com a mesma regra de arquivar do resto do app: quem **nunca serviu** some de vez (perfil, vínculos, login — e a igreja junto, se era a última pessoa dela); quem **já serviu** perde o login e o perfil vira inativo, mantendo só o nome, que é do que o relatório precisa. Recusa o único admin da igreja e o único líder de ministério que ainda tem gente. A tela pede para digitar EXCLUIR e chama `sair()` depois.
  - Migration `20260804120000_arquivar_e_editar.sql`: `ativo` em `eventos` e `funcoes`, a policy `envios_delete` que faltava, 4 funções de contagem `security definer` e o trigger `membros_ministerio_protege_ultimo_lider`.
- Detalhes da 6.2/6.3 que valem lembrar:
  - **Contar histórico tem que ser `security definer`.** `escalacoes_select` só mostra as escalações do ministério que a pessoa lidera (ou as publicadas). Se o app contasse pelo cliente, um líder olhando um evento com **rascunho** de outro ministério leria zero, o app ofereceria "excluir de vez" e o cascade apagaria o trabalho alheio. Daí `contar_escalacoes_do_evento/_do_ministerio/_da_funcao` e `contar_usos_da_musica`. Há teste de integração exatamente para isso.
  - **Trigger de cascade precisa de saída.** `trg_protege_ultimo_lider` roda em `before delete` de `membros_ministerio`, e o cascade da exclusão de um ministério dispara nele. Sem o `if not exists (select 1 from ministerios ...) then return` no topo, excluir um ministério novo morria reclamando de líder. O Postgres apaga o pai antes de cascatear, então "o ministério sumiu" é como se detecta o cascade.
  - **`obterOuCriarEscala` era uma corrida.** SELECT-depois-INSERT: no StrictMode do React o efeito roda duas vezes e as duas inserções batiam em `escalas_evento_id_ministerio_id_key` — erro vermelho na tela de montar escala a cada carregamento em desenvolvimento, e um risco real com dois líderes na mesma URL. Agora trata `code === "23505"` relendo a linha de quem ganhou. **Achado só no navegador.**
  - **Renomear coluna do repertório não mexe na `chave`.** `atualizarCampoMusica` só troca o `rotulo`: a chave indexa `musicas.extras`, e reescrevê-la exigiria migrar o jsonb de todo o repertório sem transação — meia falha deixaria valores órfãos. O líder nunca vê a chave.
  - **O admin não tinha caminho para montar escala.** Ele é `e_lider` de tudo no banco (`e_lider()` cai em `e_admin()`), mas não é *membro* de nenhum ministério, então `listarMinisteriosLideradosPor` devolvia vazio e a Agenda não mostrava atalho nenhum. `Eventos.tsx` agora lista todos os ministérios quando `papelGlobal === "admin"`.
  - **Bundle:** com as telas novas o pacote único foi a 618 KB. Agora **só `Login` e `Home` nascem no pacote principal** e todo o resto entra por `React.lazy` — carga inicial **581 KB** (gzip 175 KB), precache com 45 entradas e o `.woff2` dentro.
  - `exigirLinhaAfetada` (`packages/core/src/api/linhas.ts`) é obrigatório em toda escrita: RLS que filtra tudo faz o PostgREST responder 200 com zero linhas, e sem a checagem o botão "funciona" e o item volta.
  - **Dá para mexer em `auth.users` de dentro do banco.** No Supabase hospedado o papel `postgres` **não** é superusuário, mas tem DELETE/UPDATE em `auth.users` e tem `BYPASSRLS` — conferido no projeto de produção antes de escrever a migration de exclusão de conta. Por isso uma RPC `security definer` (dona: `postgres`) resolve, sem precisar de Edge Function com service_role. Conferir de novo, se um dia falhar, com `POST /v1/projects/{ref}/database/query` e `has_table_privilege('postgres','auth.users','DELETE')`.
  - **`excluindo_a_propria_conta()` é um passe de transação.** Os triggers que impedem mexer no próprio `ativo` e a saída do último líder atrapalhariam a exclusão de conta, cujas checagens são mais específicas e com mensagem melhor. A RPC marca `set_config('app.excluindo_conta','sim', true)` (local à transação) e os dois triggers saem na frente quando veem a marca. Nunca ligue essa marca em outro lugar.
- Detalhes da 6.1 que valem lembrar:
  - **`@theme static`, não `@theme`:** o Tailwind v4 descarta a variável de tema que nenhuma utility usou, e o bloco de `prefers-reduced-motion` zera `--duracao-lenta`, que sem `static` nem existiria no `:root`.
  - **Inter entra por `@font-face` escrito à mão** apontando para `@fontsource-variable/inter/files/inter-latin-wght-normal.woff2` (o Vite resolve o especificador do pacote dentro de `url()`). Importar o `wght.css` do pacote traria 7 subconjuntos = 213 KB para o precache; só o latin (48 KB) cobre todo o português.
  - **`gen_random_bytes` mora no schema `extensions`** no Supabase, não em `public`. Função `security definer` que usa pgcrypto precisa de `set search_path = public, extensions` — só `public` responde "function does not exist". O `gen_random_uuid` engana porque é embutido no Postgres 13+.
  - **Bundle:** o pacote único passou de 464 KB para 610 KB (culpa do `motion`), acima do teto de 600 KB do plano. Resolvido com `React.lazy` — ver o item da 6.2 acima, que ampliou a divisão para quase todas as rotas.
  - **Erro do PostgREST não é `instanceof Error`** — é objeto simples (`{code, message, details, hint}`). O `mensagemDeErro` de `apps/web/src/lib/erros-auth.ts` testava só `instanceof Error` e engolia toda mensagem que o banco escreve em português ("Este convite venceu.") trocando pelo texto genérico. Achado só rodando no navegador, não pelos testes. Todo `catch` novo tem que passar por `mensagemDeErro`, nunca por `error.message` direto.
  - O `injectManifest` agora declara `globPatterns` com `woff2` — sem isso a fonte ficava fora do precache e o app offline voltava para a fonte do sistema, sem quebrar o build.
  - Ícone do PWA repintado na cor da marca (`gerar-icones.mjs` + `pwa-icon.svg`); os 4 PNGs foram regerados.
- **Verificado no Chrome, 6.1:** cadastro → onboarding → criar igreja → criar ministério → gerar código → sair → abrir `/cadastrar?convite=CODIGO` → segunda conta entra **direto na Home**, já dentro do ministério. Conferido também: recarregar `/ministerios` não salta mais para `/onboarding`; código inválido mostra a mensagem exata do banco; digitar o código em minúscula com hífen funciona; membro comum não lê a tabela `convites` e leva 400 ao tentar `papel_global: "admin"`; rota `*` renderiza dentro do Layout.
- **Verificado no Chrome, 6.2/6.3:** excluir de vez um ministério sem histórico ("Mídias" sumiu da lista); arquivar um com histórico (o diálogo disse "já aparece em 6 escalas"), ver na aba **Arquivados** e desarquivar; renomear a função "Baixo" → "Baixo elétrico"; pedir para excluir "Vocal" e receber a oferta de arquivar ("8 escalações"); corrigir a data de um evento (19:30) e ver a lista reordenar; `/conta`, `/relatorio` e `/repertorio` renderizando; console limpo. **Falta só o roteiro de celular de verdade** (instalar, modo avião, safe-area da barra inferior) — a extensão não consegue redimensionar a janela do Chrome.
- **Atenção ao `apps/web/.env`:** ele aponta para o Supabase local pelo **IP da rede** (para o celular alcançar), e esse IP muda com o DHCP. Quando o app fica "mudo" no login sem erro no console, é isso: confira com `Get-NetIPAddress` e atualize `VITE_SUPABASE_URL`. (Hoje: `192.168.68.107`.)
- Fase 5 (acabamento) entregou três coisas: **relatório de participação** (`/ministerios/:id/relatorio`, só para líder/admin), **leitura offline** e **instalação como app**. Detalhes nos itens abaixo.
- **Próximo passo: commitar a Etapa 6 e publicar.** O plano `planejamento/etapa-6-conta-edicao-redesign.md` nasceu de três reclamações do usuário — (1) não existe cadastro de verdade, (2) dá para criar e nunca desfazer, (3) o front está feio — e as três estão resolvidas. Decisões fechadas com ele: **código de convite gerado pelo líder, com validade e limite de usos** (nada depende de e-mail); **cadastro sem confirmação** de e-mail; **arquivar o que tem histórico** e excluir de vez só o que nunca foi usado; paleta **azul-petróleo `#0F766E` + cinza**, fonte **Inter**; movimento **"vivo + efeitos de superfície"**, respeitando `prefers-reduced-motion`.
- **Ordem combinada com o usuário: web primeiro, app executável depois.** O app nativo/desktop (a "Fase 6" do `planejamento/planejamento.md`) só entra depois que a web estiver pronta e publicada — é o que ele pediu explicitamente.
- O relatório conta só escala **publicada** e mostra quem serviu zero vez (metade da utilidade é essa). A conta do período respeita o fuso da igreja: `intervaloDeDatasLocais` em `packages/core/src/datas.ts` garante que o culto de domingo à noite do dia 31 não escorregue para o mês seguinte. A agregação pura é `relatorio.ts`; quem consulta é `api/relatorio.ts`.
- `packages/core/src/datas.ts` virou a casa das contas de fuso (antes estavam em `lembretes.ts`). **Nem `datas.ts` nem `lembretes.ts` podem importar outro módulo do core**: a Edge Function os importa por caminho relativo e o Deno não resolve os `.js` que o TypeScript exige aqui.
- Offline: o service worker guarda as respostas de `GET /rest/v1/` com NetworkFirst (5s de espera) e serve o app pelas rotas internas via `NavigationRoute`. O logout apaga esse cache (`apps/web/src/lib/offline.ts`) — celular emprestado na igreja não pode mostrar a escala de quem logou antes. Escrever (confirmar presença, publicar) continua exigindo rede, e o `AvisoOffline` no Layout diz isso.
- Ícones PNG do PWA saem de `node apps/web/scripts/gerar-icones.mjs` (rasterizador próprio, sem dependência nova) — 192, 512, maskable 512 e apple-touch-icon, todos versionados. Rode de novo só se o desenho do ícone mudar.
- Fase 2 entregou: tela "Minha disponibilidade" (`/disponibilidade`), confirmar/recusar presença em "Minhas escalas", painel do líder com status de confirmação em "Montar escala", regras do ministério configuráveis (`MinisterioDetalhe`), e `validarEscalacao` chamado de verdade antes de salvar uma escalação (bloqueios impedem, avisos só informam).
- Fase 3 entregou: compartilhar a escala no WhatsApp (`texto-escala.ts` + botão em Montar escala, registrando na tabela `envios`); push do PWA (service worker próprio em `apps/web/src/sw.ts` via `injectManifest`, tabela `push_subscriptions`, botão "Avisos no celular" na Home); e lembrete de véspera (Edge Function `enviar-lembretes`, com a conta de "amanhã" por fuso da igreja em `packages/core/src/lembretes.ts`).
- **O projeto já roda contra Postgres real.** `npx supabase start` (com Docker Desktop aberto) aplica as 9 migrations e o seed; `npm run test:integracao` roda 66 testes contra esse banco, incluindo o isolamento entre ministérios que o planejamento chama de teste mais importante. `npm test` roda 79 testes puros e não precisa de Docker.
- A suíte de integração **precisa de banco limpo**: `fluxo.integracao.test.ts` conta linhas do seed e quebra se rodar duas vezes seguidas. Rode `npx supabase db reset` antes. (`relatorio`, `convites` e `arquivar` são as exceções: o primeiro limpa a própria janela de março/2025; os outros dois montam uma igreja nova a cada execução, justamente para não mexer nos números que os demais conferem.)
- Subir o banco revelou dois bugs que travavam o app inteiro e que só apareceriam em produção — ver `memoria/memoria.md`: (1) o seed criava usuários com colunas de token `null`, e o GoTrue derrubava **todo login**; (2) faltavam os GRANTs de tabela para `authenticated`/`service_role`, então toda query respondia "permission denied" mesmo com RLS correta. Ambos corrigidos.
- A Edge Function `enviar-lembretes` já foi exercitada de verdade com `supabase functions serve`: achou a escalação do dia seguinte (filtro por fuso funcionando — o evento a 10 dias ficou de fora), buscou os aparelhos e chamou o `web-push` com chaves VAPID reais, falhando só na criptografia porque as chaves do aparelho eram falsas.
- **O que ainda não foi verificado:** entrega real de push num aparelho (precisa de inscrição de navegador de verdade) e a Fase 5 num navegador de verdade (instalar o app, abrir offline, Lighthouse). O agendamento diário **já está no ar** (cron `lembretes-vespera`), e o guarda de segredo da Edge Function foi exercitado em produção: sem o `x-lembretes-secret` responde 401, com ele responde `{"ok":true,"enviados":0}`. O que dá para conferir sem navegador está conferido: o `sw.js` gerado tem as duas rotas e o precache com os 10 arquivos, e o manifest sai com os ícones certos.
- Antes de usar push é preciso gerar o par VAPID (comando no `.env.example` e no fim de `enviar-lembretes/index.ts`): a pública vai em `VITE_VAPID_PUBLIC_KEY`, a privada vira segredo da Edge Function. Sem a chave, o botão "Avisos no celular" simplesmente não aparece.
- **Cuidado com o build:** `apps/web/src/lib/supabase.ts` faz `throw` no topo do módulo. Sem `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY`, o Vite inlineia as variáveis vazias, o `throw` vira incondicional e o Rollup descarta o app inteiro — o build saía com código 0 e um bundle de 182 KB só com bibliotecas. Corrigido em `vite.config.ts`, que agora aborta o build com mensagem clara. Para buildar local: copie `apps/web/.env.example` para `apps/web/.env`. Bundle saudável tem ~464 KB (e o `sw.js`, ~25 KB).
- Docker Desktop está instalado mas não sobe sozinho: abra o app antes de `npx supabase start`. Depois de mexer em migration ou no seed, use `npx supabase db reset` e rode a suíte de integração de novo.
- O `apps/web` agora tem três tsconfigs: `tsconfig.app.json` (DOM, exclui `src/sw.ts`), `tsconfig.node.json` (vite.config) e `tsconfig.worker.json` (WebWorker, só o service worker). `tsc -b` cobre os três.
- **Produção:** app em https://escala-app-two.vercel.app (projeto Vercel `escala-app`, ligado ao GitHub — todo push para `main` publica sozinho). Banco: projeto Supabase `escala-app-sp` (`ehwvgsyrtymdypubnhjr`), São Paulo, com as 5 migrations e **sem seed**. Cron `lembretes-vespera` ativo às 21h UTC (18h de Brasília). As **chaves legadas do Supabase estão desligadas**: `VITE_SUPABASE_ANON_KEY` guarda a chave publicável (`sb_publishable_...`), apesar do nome. Runbook completo em `DEPLOY.md`.
- **Armadilha que já custou uma sessão inteira:** no PowerShell 5.1, `"valor" | vercel env add NOME` grava um BOM (U+FEFF) invisível na frente do valor. A chave vai num cabeçalho HTTP, cabeçalho só aceita Latin-1, e o `fetch` recusa a requisição inteira antes de sair do navegador — o app fica 100% mudo, sem uma linha de log no servidor. Grave variáveis pelo Bash (`printf '%s'`) ou pelo painel. `apps/web/src/lib/supabase.ts` hoje também limpa BOM e espaços, mas o melhor é não gravar torto.
- **Onboarding ainda não foi concluído em produção:** existe 1 usuário autenticado (`guibeloliv@gmail.com`, criado por link mágico, **sem senha**), 0 perfis e 0 igrejas. O banco de produção continua vazio de dados de negócio — o caminho agora é o dono criar a conta em `/cadastrar` e passar pela porta "Sou eu quem administra a igreja" do onboarding.

## Como trabalhar neste projeto

- Perguntar antes de fazer commit/push, mesmo que já tenha sido autorizado antes — cada vez é uma autorização nova.
- Perguntar antes de criar recursos em contas externas (Supabase cloud, Vercel).
- Seguir os documentos em `planejamento/` como fonte de verdade de produto/arquitetura.

## O que é o app

App para organizar a escala de pessoas que servem na igreja, separadas por ministério, deixando o processo mais simples e organizado.

## Decisões importantes

- Cada pessoa pertence a um ou mais ministérios e só pode ser escalada no ministério ao qual pertence.
- Os líderes de cada ministério montam e editam a escala do próprio ministério direto pelo app, sem depender de um administrador central.
- Plataforma: web responsivo / PWA primeiro; app nativo (Play Store/App Store) é objetivo futuro.
- Stack definida: React + TypeScript + Vite + Tailwind no front, Supabase (Postgres + Auth + Row Level Security) no back. Detalhes completos em `planejamento/arquitetura.md`.
- Escopo do produto e roadmap por fases: `planejamento/planejamento.md`.

## Estrutura do repositório

- `memoria/` memória detalhada do projeto (contexto, decisões, histórico)
- `planejamento/` planejamento de produto e arquitetura técnica
- `regras/` regras de negócio da escala
- `ferramentas/` ferramentas e integrações usadas pelo app
- `ministerios/` cadastro de ministérios e líderes
- `pessoas/` cadastro de pessoas por ministério
- `escalas/` escalas geradas, por período
- `repertorio/` biblioteca de louvores predefinidos para montar o cronograma do ministério de louvor

## Pendências

Ver seção "Estado atual" no topo deste arquivo.
